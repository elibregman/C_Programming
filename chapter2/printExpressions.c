#include <stdio.h>

int main(void)
{
    /* using printf to print out complete expressions with logic inside of the function */

    int x = 5, y = 5, z = 5;
    printf("x * y * z = %d\n", x * y * z);
    return 0;
}
